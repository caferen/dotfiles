[Unit]
Description=Backup files

[Service]
User=eren
Restart=always
RestartSec=15m
ExecStart= /bin/bash -c "/usr/bin/rsync -a --delete $HOME/ssd/drive-cipher $HOME/backup"

[Install]
WantedBy=multi-user.target
