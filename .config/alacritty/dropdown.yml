shell:
  program: /bin/zsh
  args:
    - -c
    - "tmux attach -t Dropdown || tmux new -s Dropdown"

font:
  size: 13.0
  normal:
    family: MesloLGS NF

window:
  decorations: None
  padding:
    y: 10

import:
  - ~/.config/alacritty/base16-windows-95.yml
